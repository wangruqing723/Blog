server:
  port: 8109
spring:
  application:
    name: blog
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_blog?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT
    username: root
    password: wyong
  thymeleaf:
    cache: false

mybatis:
  type-aliases-package: com.wy.blog.pojo
  mapper-locations: classpath:/mapper/*.xml

logging:
  level:
    root: debug
